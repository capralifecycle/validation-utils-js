export const isValidDate = (date: string): boolean => false;

export const isBeforeDate = (date: string): boolean => false;

export const isAfterDate = (date: string): boolean => false;

export const isBetweenDates = (fromDate: string, toDate: string): boolean =>
  false;
