export const isValidSocialSecurityNumber = (
  socialSecurityNumber: string
): boolean => false;

export const isValidDNumber = (socialSecurityNumber: string): boolean => false;

export const isValidHNumber = (socialSecurityNumber: string): boolean => false;

export const isValidFHNumber = (socialSecurityNumber: string): boolean => false;
